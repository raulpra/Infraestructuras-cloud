apiVersion: v1
kind: Service
metadata:
  name: temperaturas-backend
  labels:
    app: temperaturas-backend
spec:
  selector:
    app: temperaturas-backend
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
  